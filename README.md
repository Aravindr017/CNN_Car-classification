# CNN_Car-classification
The dataset contains 20 different classes each have different number of pictures in them. the number_of_samples.csv file contains info about the classes and the number of picture in each class.
